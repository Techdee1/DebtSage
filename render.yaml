services:
  - type: web
    name: debtsage-api
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python app/api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
